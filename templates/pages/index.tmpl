{{ define "pages/index.tmpl" }}
<!DOCTYPE html>
<html lang="en">
  <head>
    {{ template "partials/meta" }}
    {{ template "partials/styles" }}
  </head>

  <body>
    <main>
      <!-- Album Art -->
      <img src="/thumbnail/{{ .DefaultTrack.ID }}">

      <!-- Player Controls -->
      <div>
        <button>Play</button>
      </div>

      <!-- Track List -->
      <ol>
        {{ range $track := .Tracks }}
          <li>{{ $track.Tag.Title }}</li>
        {{ end }}
      </ol>

      <script src="/assets/js/app.js?v={{ ran_at }}"></script>
    </main>
  </body>
</html>
{{ end }}
